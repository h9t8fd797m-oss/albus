Check supabase/functions/_tests/body_test.ts
Check supabase/functions/_tests/chat_prompt_test.ts
Check supabase/functions/_tests/codes_test.ts
Check supabase/functions/_tests/curriculum_test.ts
Check supabase/functions/_tests/grade_prompt_test.ts
Check supabase/functions/_tests/knowledge_test.ts
Check supabase/functions/_tests/prompt_test.ts
Check supabase/functions/_tests/revenuecat_test.ts
Check supabase/functions/_tests/schema_test.ts
Check supabase/functions/_tests/signals_test.ts
running 5 tests from ./supabase/functions/_tests/body_test.ts
bounded JSON reader accepts a body below its byte ceiling ... ok (15ms)
bounded JSON reader refuses an announced oversized body before reading ... ok (639µs)
bounded JSON reader stops a streamed body that lies about its size ... ok (596µs)
bounded raw reader cancels an unannounced oversized chunked body ... ok (353µs)
bounded JSON reader gives malformed input a safe public error ... ok (437µs)
running 22 tests from ./supabase/functions/_tests/chat_prompt_test.ts
system prompt states progress and lists the plan ... ok (615µs)
saved task text cannot close the assignment context fence ... ok (387µs)
system prompt refuses off-topic work and ghostwriting ... ok (73µs)
a student's own curriculum is in scope, not off-topic ... ok (74µs)
history is capped so a client cannot inflate cost ... ok (163µs)
oversized history messages are truncated ... ok (60µs)
malformed history entries are dropped, not trusted ... ok (79µs)
history must start with a user turn ... ok (64µs)
non-array history yields nothing ... ok (41µs)
curriculum and subjects reach the prompt ... ok (174µs)
no student context adds nothing rather than an empty heading ... ok (70µs)
a curriculum with no courses still says what it knows ... ok (71µs)
focusing a step names it, so the answer is about that step ... ok (65µs)
an out-of-range focus is ignored rather than crashing the prompt ... ok (66µs)
the whole plan is still present when one step is focused ... ok (57µs)
a subject name cannot spread across lines in the system prompt ... ok (90µs)
subject names are capped and the list is bounded ... ok (203µs)
no retrieved reference leaves the message exactly as it was ... ok (56µs)
retrieved sections arrive fenced, numbered and attributed ... ok (74µs)
a student cannot close the reference fence and write their own ... ok (64µs)
a forged student_message tag cannot split the question ... ok (55µs)
the reference block is ordered before the question ... ok (46µs)
running 3 tests from ./supabase/functions/_tests/codes_test.ts
accepts the codes the generator actually emits ... ok (610µs)
rejects anything that is not a bare code ... ok (97µs)
rejects non-strings rather than coercing them ... ok (70µs)
running 1 test from ./supabase/functions/_tests/curriculum_test.ts
curriculum lookup embeds and orders topics into the prompt even without marks ... ok (973µs)
running 44 tests from ./supabase/functions/_tests/grade_prompt_test.ts
the rubric and the work are fenced separately ... ok (621µs)
a student cannot forge a closing tag to escape the work fence ... ok (274µs)
the injection rule is stated in the cacheable half ... ok (164µs)
the system prompt is identical for every grading ... ok (70µs)
a body-only rubric still produces a usable prompt ... ok (71µs)
marks above a criterion's own maximum are clamped ... ok (596µs)
the overall is the sum of the parts, not the model's own total ... ok (78µs)
an overall above the rubric's total is clamped ... ok (53µs)
negative marks become zero rather than a credit ... ok (69µs)
the rubric's total wins over whatever the model reports ... ok (61µs)
a rubric with no marks produces comments and no invented numbers ... ok (52µs)
an empty response is rejected rather than shown as a blank grade ... ok (535µs)
a runaway criteria list is capped ... ok (228µs)
non-numeric marks are dropped, not coerced to zero silently ... ok (46µs)
the work cap is a real number a client can be told about ... ok (28µs)
blind grading strips every mark the model tried to award ... ok (74µs)
a rubric grading keeps its marks ... ok (41µs)
the blind prompt never mentions a rubric ... ok (64µs)
the blind voice forbids marks, and the rubric voice does not ... ok (105µs)
blind mode still defends against injected instructions ... ok (80µs)
blind grading with nothing to say still fails rather than returning empty ... ok (65µs)
a quote is kept verbatim, with where it came from ... ok (81µs)
a quote long enough to be a paraphrase is cut ... ok (66µs)
blind grading keeps the quotes but still loses the numbers ... ok (48µs)
an improvement with no change to make is dropped ... ok (39µs)
both voices ask for the student's own sentence ... ok (101µs)
a presentation preference is fenced like every other student input ... ok (46µs)
no preference means no preference block at all ... ok (73µs)
both voices say the preference cannot move the marks ... ok (103µs)
a preference cannot forge a closing tag to escape its fence ... ok (62µs)
a preference is truncated rather than allowed to become a second brief ... ok (54µs)
blind mode is told to refuse marks even if the preference asks for them ... ok (53µs)
normalising strips what costs tokens and keeps what carries meaning ... ok (234µs)
normalising leaves a number inside a sentence alone ... ok (32µs)
normalising a realistic extract is a material saving ... ok (90µs)
the hash changes when what it was marked against changes ... ok (2ms)
at most three improvements are returned ... ok (60µs)
a blind reading cannot carry a grade, however hard the model tries ... ok (51µs)
the student's own scale is what comes back ... ok (69µs)
there is always a headline when marks exist ... ok (47µs)
a grade that arrives as a paragraph is not a grade ... ok (51µs)
the assignment title is fenced like everything else the student wrote ... ok (47µs)
a note that runs long is cut at a word, not mid-syllable ... ok (82µs)
a grade label carries the grade and not the marks ... ok (36µs)
running 7 tests from ./supabase/functions/_tests/knowledge_test.ts
no corpus means no query at all, not an empty one ... ok (645µs)
an empty question is not searched either ... ok (62µs)
the corpus and question are passed as parameters, never interpolated ... ok (80µs)
a failed lookup answers without the corpus rather than failing ...
------- post-test output -------
knowledge lookup failed, answering without it: connection reset
----- post-test output end -----
a failed lookup answers without the corpus rather than failing ... ok (1ms)
a section too large for the remaining budget is dropped, not truncated ... ok (142µs)
total retrieved text stays under the ceiling however many rows come back ... ok (104µs)
rows with no body are skipped rather than fenced as empty reference ... ok (70µs)
running 32 tests from ./supabase/functions/_tests/prompt_test.ts
routes rubric-bound work to the stronger model ... ok (576µs)
routes generic work to the cheap model ... ok (43µs)
a rubric with no criteria is treated as generic ... ok (57µs)
system prompt embeds every criterion code ... ok (164µs)
system prompt is stable across tasks — the cache depends on it ... ok (655µs)
system prompt leaks no task specifics ... ok (65µs)
deadline is phrased in human terms ... ok (278µs)
a past deadline clamps to zero rather than going negative ... ok (42µs)
hours render without a trailing .0 ... ok (68µs)
a personal rubric routes to the stronger model ... ok (43µs)
a body-only personal rubric still counts as grounded ... ok (44µs)
an empty personal rubric is treated as generic ... ok (43µs)
a personal rubric stays out of the cacheable system prompt ... ok (69µs)
a personal rubric reaches the user prompt, fenced ... ok (154µs)
notes reach the model instead of being silently dropped ... ok (132µs)
blank notes add no empty fence ... ok (70µs)
a student cannot close the fence early to escape it ... ok (94µs)
the fencing rule is stated above the cache breakpoint ... ok (39µs)
priority colours the advice without inventing a schedule ... ok (56µs)
an exam paper with only objectives still counts as grounded ... ok (35µs)
objectives and their weightings reach the system prompt ... ok (76µs)
a paper with no criteria is told not to invent criterion codes ... ok (166µs)
weighting drives emphasis, not just decoration ... ok (90µs)
a criteria-bearing component still maps steps onto criteria ... ok (57µs)
an empty curriculum context is still not grounded ... ok (39µs)
the prompt tells the model to prefer the specific need over the generic one ... ok (27µs)
step count scales with the work, instead of a fixed range ... ok (1ms)
the sitting limit follows the student's daily capacity ... ok (51µs)
a tiny task is not padded into a plan ... ok (38µs)
the planner is told how long it has, not just how much work ... ok (35µs)
fencing protects a tag nobody has invented yet ... ok (42µs)
syllabus grounding is bounded and absent when empty ... ok (184µs)
running 11 tests from ./supabase/functions/_tests/revenuecat_test.ts
RevenueCat HMAC accepts the exact signed bytes ... ok (2ms)
RevenueCat HMAC rejects a changed body and wrong secret ... ok (417µs)
RevenueCat HMAC rejects stale and malformed delivery timestamps ... ok (128µs)
RevenueCat timestamps never turn invalid data into no-expiry ... ok (92µs)
RevenueCat environment accepts only the documented enum ... ok (60µs)
constant-time comparator still compares value and length ... ok (47µs)
only expiration revokes immediately ... ok (34µs)
RevenueCat events must belong to an explicitly allowed app ... ok (87µs)
an unmapped product is loud and retryable, not a silent 200 ... ok (72µs)
outcomes a retry cannot fix are never retried ... ok (65µs)
ordinary outcomes stay quiet so the loud ones keep meaning something ... ok (164µs)
running 10 tests from ./supabase/functions/_tests/schema_test.ts
accepts a well-formed plan unchanged ... ok (747µs)
rejects responses with no usable steps ... ok (532µs)
rejects a step with no title — unstartable by definition ... ok (141µs)
clamps absurd durations instead of failing the request ... ok (379µs)
coerces a non-numeric duration to a sane default ... ok (94µs)
scales down a plan that overshoots the student's budget ... ok (117µs)
caps runaway step counts ... ok (113µs)
a step longer than one sitting is split, not clamped ... ok (109µs)
the sitting limit follows the student, not a constant ... ok (84µs)
normalises an empty criterion code to null ... ok (49µs)
running 18 tests from ./supabase/functions/_tests/signals_test.ts
an IPv4 address is reduced to its /24 ... ok (31ms)
two addresses in one /24 produce the same prefix ... ok (254µs)
different /24s stay different ... ok (183µs)
an IPv6 address is reduced to its /48 ... ok (113µs)
a client prepending a forged hop cannot change its own prefix ... ok (176µs)
a missing or unparseable forwarded-for yields no signal ... ok (857µs)
the device header is accepted only in the shape of an IDFV ... ok (1ms)
a hash is 64 hex characters and does not contain its input ... ok (734µs)
the same input hashes the same way twice, different inputs do not ... ok (338µs)
not on your plan is 402 and says so ... ok (709µs)
an allowance you bought and used is 402, not a rate limit ... ok (90µs)
going too fast is 429, and stays distinct from running out ... ok (150µs)
the outer request flood gate is a retryable 429 ... ok (54µs)
monthly AI safety ceiling is distinct from rate and entitlement ... ok (104µs)
a risk refusal never names the signal that caused it ... ok (160µs)
verification is 403 and offers a way forward ... ok (61µs)
an unmapped Postgres error reaches the client as nothing at all ...
------- post-test output -------
unmapped postgres error: duplicate key value violates unique constraint "entitlements_pkey"
----- post-test output end -----
an unmapped Postgres error reaches the client as nothing at all ... ok (283µs)
the old FREE_PLAN_LIMIT_REACHED name still maps ... ok (61µs)

ok | 153 passed | 0 failed (582ms)

