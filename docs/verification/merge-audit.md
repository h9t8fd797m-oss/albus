# Curriculum hand-merge audit — 8 September 2026

Verdict: the curriculum hand-merge is clean. No content correction is warranted. All 19 subject JSON objects equal the exact union of the two authored changes: 304 top-level fields and 115 notes checked, including ordered nested arrays, complete strings and duplicate-key detection. Nothing in the landed JSON was invented during conflict resolution; no authored note was dropped.

## What Git actually contains

`24a9f2e421a207903afd31a30e5912bae46ebb50` is a squash commit with **one** parent, `d439e3369cc313d1113aea390ffaf35f82adc87e`, the landed #48. There is no second parent to inspect. The original independent authored heads remain available:

- #48: `45cbe5c1b36a57b33e9df895475a494a31174755`.
- #50: `1a609f704eedcfe9e6ef124a48ca64bbf2b0024a`.
- Common base: `e0dd0b595f313d4f460bfa06c20af3659cfc147a`.
- Rebased #50 before output repair: `1073234e83df9c20aa775968c363d81a5267f99c` (parent `d439e33`).
- Output repair: `e502286fececef15d436a63b159e615790cb0ab3`.

`git diff 45cbe5c d439e33` contains only the independently landed week-fit changes, with no curriculum differences. The complete Git tree of `e502286` is identical to `24a9f2e`: `4d33dc16066fa297939ebcf7cb67b98b77ef7eff`.

## Exhaustive field accounting

Run `python3 scripts/curriculum/audit_20260908_merge.py`. The script reads immutable Git objects, rejects duplicate JSON keys before comparison, and derives the expected result without consulting the landed values. Unchanged fields come from the common base; independently changed fields come from their author; equal changes collapse to one value. Notes must preserve the entire base prefix and then exactly retain both branches' additions in order. Any conflicting authored value or unexplained landed field fails.

[The full output](merge-audit.txt) lists every subject and every changed field. It confirms all original assessment components, criteria, weights, source URLs and metadata survive as well as the explicitly requested topics and objectives. Business criteria were compared for preservation only; they were not researched or changed.

History retains all 24 topic rows in their original order, the topic source and confidence, all seven combined notes, and its existing assessment components. `lastAssessment: 2027` and `supersededBy: "First assessment 2028"` agree in the common base and both original PR heads, and occur once each in the landed JSON. Thus the conflict resolution preserved the intended course vintage; this audit does not claim a new external revalidation of the guide. The 101-line addition seen against original #50 consists of #48's topic additions and its note, with no unrelated loss.

Biology retains **4** topic rows, Chemistry **8**, and Physics **29**, exactly as #48 authored them. These are the authored outlines, not proof of every syllabus subtopic: Biology's own retained note explicitly limits its four rows to the public organising themes. Each retains all four #50 objectives in AO1–AO4 order, with null weightings, the objective source string and `corroborated` confidence unchanged. Topic sources/confidences match #48; objective sources/confidences match #50. No source crossed fields.

All 19 subjects were checked for `official` or `corroborated` confidence on an empty topics/objectives array: none. The exact note union also proves no absence note was silently lost.

## Generated-output repair

`git diff --name-only e502286^ e502286` returns only:

```
ios/App/Albus/Models/Curriculum.swift
scripts/curriculum/seed.sql
```

Both `python3 scripts/curriculum/gen.py --check` and `python3 scripts/curriculum/gen.py` returned `✓ 19 subjects valid`; [their complete output](merge-audit-generator.txt) is committed. Immediately afterward, `git diff --exit-code -- ios/App/Albus/Models/Curriculum.swift scripts/curriculum/seed.sql` returned exit 0 with no output. The fresh generated bytes therefore match the committed files exactly; nothing from the intermediate stale state remains.

Fresh-file SHA-256 values:

```
515b5a1d9a07ec5206c3ea3d8548d94bdce8f496675e01810bd69ee29369dcc5  ios/App/Albus/Models/Curriculum.swift
62aadb9b8f0ccb53004dce9acf57e06ea23cea98665000aaeed0e250aa943386  scripts/curriculum/seed.sql
```

This part changes only audit tooling and evidence. It makes no new curriculum claim and performs no production operation. Database, edge and Apple-platform runtime verification belongs to the coordinated release checks; it is not implied by this forensic verdict.
